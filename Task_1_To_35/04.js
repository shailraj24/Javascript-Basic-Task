// 4. Check if the string contains a word Script using includes() method.

let sampleString = 'There is immense scope of scripts used in scripting languages and scripts are considered best as javascript and I know that this sentence makes no Scripty sense, it is just a sample Scriptiness test';

const word = 'Script';

console.log(`Does the sampleString contain the word : ${word} ? ${sampleString.includes(word) ? 'Yes' : 'No'}`);

