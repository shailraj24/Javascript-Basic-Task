// 1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.


let myName = 'manik';
let flag = true;
let tag;
let location = null;
// let salary = 1000;

console.log(`Value of myName is ${myName} and type is ${typeof myName}`);
console.log(`Value of flag is ${flag} and type is ${typeof flag}`);
console.log(`Value of tag is ${tag} and type is ${typeof tag}`);
console.log(`Value of location is ${location} and type is ${typeof location}`);
// console.log(`Value of salary is ${salary} and type is ${typeof salary}`);