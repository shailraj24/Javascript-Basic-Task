// 6. 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon' split the string at the comma and change it to an array.

let sampleString = 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon';

console.log(sampleString.split(',')); //Does not include Single Space after comma

console.log(sampleString.split(', ')); //Includes Single Space after comma

